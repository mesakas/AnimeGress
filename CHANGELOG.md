# Changelog

## [Unreleased]

- Renderer Feature 新增自动、桌面端完整效果和移动端优化三种质量档位；自动档在 Android/iOS 使用移动路径，桌面平台保留全部现有效果。
- 移动端可以独立保留地表缓存响应、实时 GrassVolume、主光源阴影、远景覆盖和远景动态明暗图案；关闭项通过 Shader 变体移除对应采样、插值与循环。
- 实体草渲染缓存单株基础矩阵，LOD 选择改为直接提交当前及过渡级，并增加分块级距离剔除。
- Renderer Feature 改为每台摄像机只绑定一次地表缓存与 GrassVolume 全局状态，减少多草场场景的重复工作。
- LOD 新增“仅 XY 距离”和“仅水平 XZ 距离”模式，可分别忽略 Z 或 Unity 的 Y 高度差；新增远景草接管选项，可保留后续面片 LOD 配置但停止提交，并让最后实体 LOD 直接渐隐到远景覆盖。
- 远景草覆盖新增对应的距离计算模式，并在自动匹配时忽略已由远景草替换的 LOD，保证高空视角下真实草和远景覆盖使用相同距离规则。
- LOD 切换改为前后两级同时提交的互补随机点状渐变，Scene 编辑预览与运行时保持一致，并自动连接相邻 LOD 的开始/结束距离。
- 草材质新增独立 Alpha 贴图、单独的 Tiling/Offset 和 `R / Alpha` 通道选择，支持不使用基础颜色贴图的纯遮罩草叶。
- 新增固定世界范围的地表属性缓存，保存颜色、世界法线、高度和排除草遮罩。
- 支持 MeshRenderer、SkinnedMeshRenderer 和最多 8 层的 Unity Terrain 捕获。
- 新增地表来源覆盖、地表遮罩 Volume、四种更新模式和中文缓存诊断界面。
- 草 Shader 新增地表颜色、法线与排除草响应。
- 新增共享 `AnimeSurfaceCache.hlsl`，供项目内其他 Shader 读取缓存。
- 修正中性灰色地表无法明显影响草根颜色的问题，并增加缺少缓存时的材质 Inspector 警告。
- 铺设笔刷新增可分别配置 X、Y、Z 轴的随机缩放比例和随机旋转比例。
- 修正使用顶部正交缓存时地表颜色沿世界 Z 轴镜像、导致草读取到相反位置颜色的问题。
- 新增 `AnimeGrassFarField` 远景覆盖，在最后一级 LOD 剔除后保留草场颜色、风场颜色和低成本伪阴影。
- 远景覆盖使用草实例颜色/高度缓存和摄像机深度重建，可过滤上下楼层、悬空遮挡物与陡峭侧面。
- 草场 Inspector 新增远景覆盖创建入口，并提供完整中文配置、缓存预览和手动重建工具。
- 远景伪阴影新增世界空间扰动，可配置扰动强度、斑块尺寸和沿风向移动速度。
- 地表交互遮罩改为球形/盒形三维 Volume，并根据缓存地表高度过滤写入，避免二维投影同时影响错误楼层。
- 遮罩 Volume 新增实时 GPU 草叶排斥，可配置推动距离、边缘过渡和受影响起始高度；Volume 界面精简为排除草与草叶推动。
- Volume 新增常驻三维范围、选中填充以及排除/推动完整强度内轮廓，并在 Inspector 显示世界包围盒和高度范围。
- Volume 范围改用始终通过深度测试的 Scene Handles 绘制，避免轮廓被高密度草叶和地形完全遮挡。
- Volume 范围默认只在选中时显示，并可通过“未选中时也显示”单独控制常驻轮廓。
- 地表缓存范围新增可穿透场景的三维捕获盒、中心覆盖面和尺寸/米每像素标签，并支持未选中时持续显示。
- README 补充缓存范围与质量说明，以及远景覆盖和最后一级 LOD 的无空带匹配规则。
- 远景覆盖新增近景草颜色匹配模式，同步代表色、阴影色、接收阴影强度、风色响应和主光照近似，降低 LOD 交接色差。
- 远景伪阴影升级为沿全局风向传播的弯曲波峰，可配置弧度、间距和横向弯曲尺度，并混合次级定向波与原有扰动。
- 远景覆盖新增小孔闭合与可调随机点状剔除，连续覆盖率通过不透明像素密度渐隐，不再使用半透明颜色混合。
- 远景覆盖新增可调轮廓柔和度和地表过滤边缘柔和度，消除高度容差与低多边形斜坡法线造成的硬切边。
- 远景覆盖新增最远渐隐开始距离和最大显示距离，超过上限后通过点状剔除完全停止显示。
- 草类型新增独立的近处/远处数量比例、密度过渡距离、单株柔和度和稳定随机种子，可在 LOD 之外实际减少远处提交实例数。
- 排除草 Volume 新增实时 GPU 跟随模式；挂在 Camera 或其子对象时自动启用，并同时影响真实草与远景覆盖，无需每帧重建地表缓存。
- 每一级草 LOD 新增“始终面向观察目标”，支持远景面片绕草根法线朝向指定角色 Transform 或当前渲染相机，并保持 Scene 视图独立预览。

## [1.0.0] - 2026-08-14

- 提供确定性的手工草场铺设和单株编辑工具。
- 支持面片草、模型草和多级 LOD。
- 支持 GPU Instancing、分块与摄像机视锥剔除。
- 支持点状渐显渐隐和最大显示距离。
- 支持全局风场、阵风和风场颜色变化。
- 支持 URP 主光源阴影、阴影贴地采样与漏光抑制。
- 支持 Scene 和 Game 多摄像机同时渲染。
- 修复窗口失焦恢复时草短暂消失的问题。
